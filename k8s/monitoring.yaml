apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: sentinelx-backend
  namespace: sentinelx
spec:
  selector:
    matchLabels:
      app: sentinelx-backend
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
